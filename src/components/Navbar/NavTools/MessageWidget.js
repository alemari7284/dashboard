import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import { faMessage } from "@fortawesome/free-solid-svg-icons";

export const MessageWidget = () => {
  return <FontAwesomeIcon className="faIcon" icon={faMessage} />;
};
