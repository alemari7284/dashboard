import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import { faClipboard } from "@fortawesome/free-solid-svg-icons";

export const Clipboard = () => {
  return <FontAwesomeIcon className="vIcon" icon={faClipboard} />;
};
