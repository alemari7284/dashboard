import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import { faClock } from "@fortawesome/free-solid-svg-icons";

export const Clock = () => {
  return <FontAwesomeIcon className="vIcon" icon={faClock} />;
};
