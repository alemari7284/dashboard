import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import { faCalendar } from "@fortawesome/free-solid-svg-icons";

export const Calendar = () => {
  return <FontAwesomeIcon className="vIcon" icon={faCalendar} />;
};
