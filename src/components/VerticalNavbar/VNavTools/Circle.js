import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import { faCircleNotch } from "@fortawesome/free-solid-svg-icons";

export const Circle = () => {
  return (
    <FontAwesomeIcon
      style={{ color: "orange", marginTop: "30px", fontSize: "2rem" }}
      icon={faCircleNotch}
    />
  );
};
